import React from 'react';

// Button component
const AmazonQLowTest3 = () => {
  const handleClick = () => {
    alert('Button clicked!');
  };

  return <button onClick={handleClick}>Click me</button>;
};

export default AmazonQLowTest3;
